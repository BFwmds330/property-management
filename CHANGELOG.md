# 更新日志（CHANGELOG）

本系统的所有重要变更都会记录在这里。版本号遵循「主版本.功能版本.修订版本」。

## 2.0.1 - 2026-09-20

### 修复

- **应缴金额为 0 元的账单（免收 / 减免到 0）不再被误判为「未缴」**：原先判定「已缴清」的条件是「实收 ≥ 应收 **且 应收 > 0**」，于是应收 0 元的账单（例如免收物业费）会被判成未缴，混进「欠费管理 / 催缴名单 / 报表欠费」，还可能给业主发催缴短信。现在 `应收 ≤ 0` 一律视为已结清，且此后对这类账单做收款或调整操作时状态也不会被重算错。
- **生成账单时就按公式定状态**：`generate_bills` 每写一笔账单立即重算状态，因此把收费项目单价设为 0（免收）后生成的账单，**一生成就是「已缴清」**，不会再出现「欠费 0 元」的杂音记录。
- **修复报表页在「0 元收费项目」下崩溃**：`templates/report/index.html` 按收费项目算收缴率时没有除零保护，只要存在应缴合计为 0 的项目（例如免收费项目），报表页会直接 500。现已与「各小区对比」一致地显示「—」。

### 说明

- 影响面仅限「应缴金额为 0」的账单与收费项目：正金额账单的「未缴 / 部分缴纳 / 已缴清」判定、欠费合计、收缴率口径**完全不变**（0 元账单对应收合计与实收合计的贡献都是 0）。
- 冒烟测试新增 7 项（0 元收费项目可生成、0 元账单生成即已缴清、0 元账单不进欠费名单、减免前为未缴、减免到 0 后变已缴清、减免后不计欠费、正金额未收款仍判未缴），基线 114 → **121 项**；其中「全部页面返回 200」这一项正是本次报表页除零崩溃的回归护栏。

## 2.0.0 - 2026-09-19（大版本：新增人员管理模块）

### 新增

- **人员管理一级导航**：管理物业企业自己的员工（区别于小区住户），含员工档案、月度工资、社保缴纳、人力成本报表四块。
- **员工档案**：姓名/性别/出生日期（年龄自动计算，不落库）/身份证号（18 位校验位校验）/手机号/部门/职位/工作小区（关联现有小区）/入职离职日期/在岗状态（在职、试用、停薪留职、离职）/合同到期/证照到期/紧急联系人/居住址/备注；支持搜索、按部门/状态/小区筛选、CSV 批量导入（下载模板 → 上传预览 → 确认导入，自动识别 UTF-8/GBK，按身份证或手机号识别已存在的员工只补空缺）。
- **月度工资批量录入**：选月份列出在职员工逐行填写；应发（基本+津贴+加班+补贴）与实发（应发−社保个人−个税−其他扣款）自动计算；社保个人代扣自动带出当月社保记录，覆盖需填原因并留痕；每人每月唯一；支持按月覆盖更新与单条删除（留痕）。
- **月度社保批量录入**：缴纳基数 + 单位五险+公积金 + 个人三险+公积金 逐列录入（允许留空=未参保），单位/个人合计自动计算，支持补缴标记。
- **人力成本报表**：按月/年度汇总应发工资、实发工资、单位社保、单位公积金与人力成本合计（实发+单位社保+单位公积金）；按部门与按员工 Top10 排名；年度工资表与社保汇总 CSV 导出。
- **总览页待办卡片新增「人员·本月台账」**：在职人数、本月工资/社保是否已录入（未录入高亮提醒直达录入页）、30 天内合同到期名单。

### 说明

- 新增数据表 `staff / staff_salary / staff_social`，老数据库启动时自动升级，对既有房屋、账单、住户数据完全无影响；
- 工资与社保属于敏感数据，请只在自用版录入（开发版仓库仅包含功能代码与虚构示例）；
- 冒烟测试扩充至 114 项。

## 1.4.0 - 2026-09-19

### 新增

- **账单可关联车辆**：账单新增可选的“关联车牌”，用于停车费等按车收费的场景；账单详情页会显示关联车牌。
- **车辆卡片显示停车费缴纳情况**：房屋详情的车辆登记卡片逐辆显示该车的历史缴费账单（账期、金额、是否缴清），并自动汇总“停车费缴至 XXXX-XX”徽章。
- 老数据文件自动升级：首次启动自动补齐账单表的关联车辆字段，无需手工处理。

## 1.3.0 - 2026-09-19

### 新增

- **业主名册批量导入**：房屋列表页新增「📥 导入名册」。流程为“下载模板 → 填写 → 上传预览 → 确认导入”，支持从 Excel 另存的 CSV（自动识别 UTF-8 / GBK 编码）。一次可导入整小区的楼栋、房屋（含建筑面积、车位号）、业主、家庭成员、租户与车辆；上传后先预览统计，确认无误才写入；已存在的房屋只补空缺、不覆盖已填数据，已有业主的房屋不会重复登记；有问题行（姓名缺失、电话/证件号不合法等）会逐行列明并拒绝导入。
- **房屋车位号**：房屋新增“车位号”字段（如“251东”），在房屋表单、列表、详情中显示。
- **车辆管理**：房屋详情新增“车辆登记”卡片，可登记多辆车（车牌号 + 车位/租期备注），支持删除；重复车牌自动拦截。

### 修复

- 无业主的空置房行不再被名册导入误判为错误（空置房也可通过名册导入建档）。
- 放宽表单内存上限至 32MB：600+ 户的名册预览数据以表单回传会超出 Werkzeug 默认 500KB 限制。

### 说明

- 本次改动附带数据库自动升级：老数据文件首次启动时会自动补齐新字段和新表，无需手工处理。

## 1.2.1 - 2026-09-19

### 新增

- **顶部随时新增小区**：导航栏小区切换器旁新增「＋ 小区」按钮，创建第一个小区后也能一眼找到入口继续添加、管理全部小区（此前该入口只能通过网址访问，不易发现）。
- **批量生成页直达楼栋管理**：「批量生成整栋房屋」页面右上角新增「🏗 新增 / 管理楼栋」按钮，还没有楼栋时下拉框下方也会显示“点这里去新增楼栋”的链接，不再需要自己找入口。
- **房屋列表页**头部新增「楼栋管理」快捷链接。
- **版本号体系**：页面底部、启动窗口、设置页均显示当前版本号；项目根目录新增 CHANGELOG.md 记录每次变更。

## 1.2.0 - 2026-09-19

### 修复

- **Windows 下两个版本（自用版/开发版）可能开在同一个端口**：旧端口探测用“试绑定 127.0.0.1”判断，会被绑定在 0.0.0.0 的另一个实例骗过，导致后启动的一版误判端口空闲而启动失败。改为“直接连接探测 + 按服务器真实方式试绑定”双重校验，端口被占时自动顺延到下一个（最多 5020），全部被占时给出明确中文提示。

## 1.1.0 - 2026-09-19

### 新增

- **版本标识机制**：项目根目录放 `edition.txt`（如“自用版”）即在标题与页面显示版本徽标；标识独立于代码文件，升级覆盖代码时不会丢失，用于复制出与开发版数据完全隔离的自用版。

## 1.0.0 - 2026-09-19

### 新增

- 首个完整版本：小区管理（多小区切换）、楼栋与房屋（批量生成）、户型库、住户管理（业主/成员/租户、历史时间线）、物业费核心（收费项目、两步生成账单、部分缴费、一键缴清、金额调整留痕、作废）、欠费与催缴短信、缴费流水、报表统计（收缴率、欠费 Top10、各小区对比）、报修登记、数据备份与恢复、演示数据一键载入/清空、操作日志、响应式界面与自动化冒烟测试。
