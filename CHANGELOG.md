# 更新日志（CHANGELOG）

本系统的所有重要变更都会记录在这里。版本号遵循「主版本.功能版本.修订版本」。

## 1.3.0 - 2026-09-19

### 新增

- **业主名册批量导入**：房屋列表页新增「📥 导入名册」。流程为“下载模板 → 填写 → 上传预览 → 确认导入”，支持从 Excel 另存的 CSV（自动识别 UTF-8 / GBK 编码）。一次可导入整小区的楼栋、房屋（含建筑面积、车位号）、业主、家庭成员、租户与车辆；上传后先预览统计，确认无误才写入；已存在的房屋只补空缺、不覆盖已填数据，已有业主的房屋不会重复登记；有问题行（姓名缺失、电话/证件号不合法等）会逐行列明并拒绝导入。
- **房屋车位号**：房屋新增“车位号”字段（如“251东”），在房屋表单、列表、详情中显示。
- **车辆管理**：房屋详情新增“车辆登记”卡片，可登记多辆车（车牌号 + 车位/租期备注），支持删除；重复车牌自动拦截。

### 修复

- 无业主的空置房行不再被名册导入误判为错误（空置房也可通过名册导入建档）。
- 放宽表单内存上限至 32MB：600+ 户的名册预览数据以表单回传会超出 Werkzeug 默认 500KB 限制。

### 说明

- 本次改动附带数据库自动升级：老数据文件首次启动时会自动补齐新字段和新表，无需手工处理。

## 1.2.1 - 2026-09-19

### 新增

- **顶部随时新增小区**：导航栏小区切换器旁新增「＋ 小区」按钮，创建第一个小区后也能一眼找到入口继续添加、管理全部小区（此前该入口只能通过网址访问，不易发现）。
- **批量生成页直达楼栋管理**：「批量生成整栋房屋」页面右上角新增「🏗 新增 / 管理楼栋」按钮，还没有楼栋时下拉框下方也会显示“点这里去新增楼栋”的链接，不再需要自己找入口。
- **房屋列表页**头部新增「楼栋管理」快捷链接。
- **版本号体系**：页面底部、启动窗口、设置页均显示当前版本号；项目根目录新增 CHANGELOG.md 记录每次变更。

## 1.2.0 - 2026-09-19

### 修复

- **Windows 下两个版本（自用版/开发版）可能开在同一个端口**：旧端口探测用“试绑定 127.0.0.1”判断，会被绑定在 0.0.0.0 的另一个实例骗过，导致后启动的一版误判端口空闲而启动失败。改为“直接连接探测 + 按服务器真实方式试绑定”双重校验，端口被占时自动顺延到下一个（最多 5020），全部被占时给出明确中文提示。

## 1.1.0 - 2026-09-19

### 新增

- **版本标识机制**：项目根目录放 `edition.txt`（如“自用版”）即在标题与页面显示版本徽标；标识独立于代码文件，升级覆盖代码时不会丢失，用于复制出与开发版数据完全隔离的自用版。

## 1.0.0 - 2026-09-19

### 新增

- 首个完整版本：小区管理（多小区切换）、楼栋与房屋（批量生成）、户型库、住户管理（业主/成员/租户、历史时间线）、物业费核心（收费项目、两步生成账单、部分缴费、一键缴清、金额调整留痕、作废）、欠费与催缴短信、缴费流水、报表统计（收缴率、欠费 Top10、各小区对比）、报修登记、数据备份与恢复、演示数据一键载入/清空、操作日志、响应式界面与自动化冒烟测试。
